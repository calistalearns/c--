#include <iostream>
using namespace std;
const int N=1e5+10;
int n,m,k,c[N],a[N],dp[N][N];
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin>>n>>m>>k;
    for(int i=1;i<=n-1;i++) cin>>c[i];
    for(int i=1;i<=n;i++) cin>>a[i];
    
    return 0;
}